process USER2 =  {
    
    # initialize  MessageLogger
    service = MessageLogger  {
	untracked vstring destinations   = { "cout" }
	untracked vstring categories     = { "PFProducer" }
	untracked vstring debugModules   = { "*" }
	untracked PSet cout = { 
	    untracked string threshold = "INFO"
	    untracked PSet INFO            = { untracked int32 limit = 0 }
	    untracked PSet DEBUG           = { untracked int32 limit = 0 }
	    untracked PSet PFProducer = { untracked int32 limit = 10000000}
	} 
    }

    
    # The particle data table
    include "SimGeneral/HepPDTESSource/data/pythiapdt.cfi"   
    

    include "RecoParticleFlow/PFClusterProducer/data/pFClusterDefaults.cff"
    include "RecoParticleFlow/PFProducer/data/pFDefaults.cff"
    include "RecoEcal/EgammaClusterProducers/data/islandClusteringSequence.cff" 
    include "RecoParticleFlow/PFProducer/data/tauHadronDecayFilter.cff"
                              
    source = PoolSource
    {
	# Number of events program should run
	untracked vstring fileNames = {'file:singletau_pf.root'}
	untracked int32 maxEvents = -1
    }

#    module test = Test {
#
#        # necessary to access true particles
#        include "FastSimulation/Event/data/NoVertexGenerator.cfi"
#        PSet ParticleFilter = {
#            # Particles with |eta| > etaMax (momentum direction at primary vertex)
#            # are not simulated
#            double etaMax = 588888.0
#            # Charged particles with pT < pTMin (GeV/c) are not simulated
#            double pTMin =  0.
#            # Particles with energy smaller than EMin (GeV) are not simulated
#            double EMin = 0.
#        }
#    }
    

    # module dump = EventContentAnalyzer {}
    
    # path p = { dump }

    
    path p = { tauHadronDecayFilter, islandClusteringSequence, particleFlowCluster, particleFlow, iterativeCone5PFJets } 
    # path p = { tauHadronDecayFilter, test} 


    # path p = { particleFlow } 

    module o2 = PoolOutputModule
    {
	untracked string fileName = "singletau_repf.root" 
	untracked vstring outputCommands = 
	{
	    "drop *",
	    "keep recoPFRecHits_*_*_*",
	    "keep recoPFClusters_*_*_*",
	    "keep recoPFRecTracks_*_*_*",
	    "keep recoPFParticles_*_*_*",			
	    "keep recoBasicClusters_islandBasicClusters_*_*",
	    "keep recoCandidatesOwned_particleFlow_*_*",
            "keep recoBasicJets_iterativeCone5PFJets_*_*",
	    "drop *_*_*_USER"
#	    "keep *_*_*_*"
	}
	untracked PSet SelectEvents = {
	    vstring SelectEvents = { "p" }
	}
    }

    
    endpath outpath = { o2 }
}
